query CartInput {
  cart {
    lines {
      id
      quantity
      cost {
        subtotalAmount {
          amount
        }
        totalAmount{
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
}
